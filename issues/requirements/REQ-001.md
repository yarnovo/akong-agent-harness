---
id: REQ-001
title: LLMClient + Session + run() 多 provider + RDS 持久化 (sync · 砍 streaming)
status: in-progress
assignee: agent-harness-maintainer
parent_scenario: SCENARIO-xiaoyan-runtime-multi-llm  # lead 派活时引用
priority: P0a
created_at: 2026-05-08
branch: feat/llm-session-streaming
---

## 描述

harness 现状是 `tick()` sync 单轮 · 直接 `OpenAI(base_url=...)` · history 不续。

升级目标 (老板 5-7 拍 + lead 调研 + 5-8 老板紧急改方向 砍 streaming)：

1. 抽 `LLMClient` Protocol · 支持 OpenAI 兼容 + Anthropic native (Pydantic AI 双层风) · sync 返完整 ChatResponse
2. 抽 `Session` Protocol · session 落 cast-api RDS 新表 chat_messages (跨 FC 实例 sticky)
3. 新 `runtime.run()` async sync 一次性返 RunResult · 多 turn tool loop (借 OpenCode processGeneration)

**5-8 老板原话**: "不用流返回" → 不 SSE 不 async generator · LLMClient.chat_completion 直接 await 返 ChatResponse · runtime.run 一次性返 RunResult

## acceptance (lead 锁 · maintainer 完工对账)

- [ ] `src/akong_agent_harness/llm.py` 含 `LLMClient` Protocol + `OpenAICompatibleClient` + `AnthropicClient` + `ChatResponse` (sync 不 stream)
- [ ] `src/akong_agent_harness/session.py` 含 `Session` Protocol + `InMemorySession` + `RdsSession`
- [ ] `runtime.py` 新 `run()` async sync (return RunResult · 不 yield) · 老 `tick()` 不动
- [ ] pytest 全过 (含 LLMClient mock + InMemorySession + run() max_turns + tool error 自纠)
- [ ] README 8 件套 + LLMClient/Session 段落
- [ ] CONTRACTS.md 上下游声明 (上 cast-api chat_messages · 下 cast-agents)
- [ ] PR 走 base 最新 main · branch=feat/llm-session-streaming

## 不做 (out of scope)

- 老 `tick()` 实现不动 (cast-app /create 链路依赖)
- context compaction (P1 二期 · OpenAIResponsesCompactionSession 同款)
- cancel/interrupt (P1)
- LiteLLM 兜底 (P2)
- telemetry / OTel (P2)

## 上游依赖

- cast-api chat_messages endpoint (P0b subagent 同步建) · 假设存在 · RdsSession 接 503/404 抛 SessionUnavailable
